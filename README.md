# Packer Setup Gentoo in VirtualBox

Build a Gentoo box with Packer.

## TL;DR

```
make
```

## Notes

- Minimal install: Just enough for VM guests
- Uses virtio instead of higher-overhead intel network interface device

